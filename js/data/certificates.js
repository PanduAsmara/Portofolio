/* =============================================================
   CERTIFICATES DATA
   -------------------------------------------------------------
   Formal certifications (e.g. BNSP, MTCNA, CCNA). These render
   into the SAME grid as achievements.js, appearing AFTER the
   achievements.

   It starts empty so the page looks exactly as before. Add a
   { ... } block to show a certificate card. The fields are the
   same shape as achievements.js:

     {
         icon: "fa-solid fa-certificate",
         iconColor: "text-cyan-400",
         badge: "Certified",
         badgeClass: "bronze",          // "gold" | "silver" | "bronze"
         title: "BNSP — Computer & Network Engineering",
         subtitle: "Valid 3 years",
         year: "2024",
         url: "https://link-to-certificate",
     }
   ============================================================= */

window.CERTIFICATES = [
    // Add formal certificates here.
];
